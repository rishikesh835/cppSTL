#include<bits/stdc++.h>
using namespace std;
int main(){
    //multimap stores multiples values with same  key
   multimap <string,int> mpp;
   mpp.emplace("rishi",23);
   mpp.emplace("rishi",65);
   return 0;
}