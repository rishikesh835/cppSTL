#include<bits/stdc++.h>
using namespace std;
int main(){
    //it is not arranged in order 
    //insertion takes constant time
    //average timecomplexity is O(1)
    //worse case complexity is O(n)

    //all funtions of sets of are valid for unordered set
    unordered_set<int> st;
    st.insert(3);
    st.insert(72);
    st.insert(14);
    st.insert(2);
    return 0;
}